import {
  color,
  fontSizes,
  fontFamily,
  fontWeight,
  lineHeights,
  space,
  screens,
  component,
} from './tokens';

import type { DefaultTheme } from 'styled-components';

export const defaultTheme: DefaultTheme = {
  color,
  fontSizes,
  fontFamily,
  fontWeight,
  lineHeights,
  space,
  screens,
  component,
};
