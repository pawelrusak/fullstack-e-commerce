import {
  color,
  fontFamily,
  fontWeight,
  space,
  screens,
  component,
} from './tokens';

import type { DefaultTheme } from 'styled-components';

export const defaultTheme: DefaultTheme = {
  color,
  fontFamily,
  fontWeight,
  space,
  screens,
  component,
};
