import {
  color,
  fontFamily,
  fontWeight,
  space,
  screens,
  component,
} from './tokens';

import type { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  color,
  fontFamily,
  fontWeight,
  space,
  screens,
  component,
};

export default theme;
